// Configuración central de la URL del backend
// Si accedes desde celular u otro dispositivo en la red, cambia localhost por la IP de tu PC
const API_BASE_URL = 'http://192.168.100.5:8080';

export default API_BASE_URL;
